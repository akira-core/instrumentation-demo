# Merge patch applied to ConfigMap demo-feature-flags in namespace demo.
data:
  flags.yaml: |
    otel-nats-tracing:
      variations:
        enabled: true
        disabled: false
      defaultRule:
        variation: enabled
