Chart: relay-proxy (GO Feature Flag relay proxy)
Repository name: go-feature-flag
Repository URL: https://charts.gofeatureflag.org/
Chart version: 1.55.1
App version: v1.55.1
Upstream source: https://github.com/thomaspoignant/go-feature-flag

Vendored via:
  helm repo add go-feature-flag https://charts.gofeatureflag.org/
  helm repo update go-feature-flag
  helm pull go-feature-flag/relay-proxy --version 1.55.1 --untar -d charts/

Pulled on: 2026-08-07 (UTC)

Do not hand-edit files under this directory except SOURCE.txt itself; to
upgrade, re-run `helm pull go-feature-flag/relay-proxy --untar -d charts`
with a new --version and update this file accordingly. Local customization
belongs in deploy/values/relay-proxy.yaml (and deploy/base/ for the flags
ConfigMap), not in this vendored chart.

Install as:  helm install relay-proxy charts/relay-proxy \
               -f deploy/values/relay-proxy.yaml -n demo
             (release name must be "relay-proxy" so the Service resolves at
             relay-proxy:1031)

Values:      deploy/values/relay-proxy.yaml
Flags:       deploy/base/feature-flags.yaml (ConfigMap demo-feature-flags;
             GOFF kind: configmap retriever + Role/RoleBinding via
             extraManifests in the values file)
